name: Build AutoHEIC

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build EXE
        run: |
          pyinstaller ^
             --onefile ^
             --noconsole ^
             --icon=icons/autoheic.ico ^
             --add-data "icons;icons" ^
             --name AutoHEIC ^
             AutoHEIC.py

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AutoHEIC-Windows
          path: dist/AutoHEIC.exe
